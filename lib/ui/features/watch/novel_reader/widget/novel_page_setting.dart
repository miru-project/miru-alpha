import 'package:flutter/widgets.dart';

class NovelPageSetting extends StatelessWidget {
  const NovelPageSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
