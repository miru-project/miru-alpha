import 'package:flutter/material.dart';

class MangaEpisodes extends StatelessWidget {
  const MangaEpisodes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
